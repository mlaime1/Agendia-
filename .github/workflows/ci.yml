name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-unit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Type check
        run: npx tsc --noEmit

      - name: Run unit tests
        run: npm run test:unit

      - name: Build
        run: npm run build

  # Integration tests require a real Postgres + Supabase Auth instance.
  # The easiest local path is `supabase start` (needs Docker). Uncomment when ready.
  # integration:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: 'npm'
  #     - name: Install Supabase CLI
  #       uses: supabase/setup-cli@v1
  #       with:
  #         version: latest
  #     - run: npm ci
  #     - run: npx prisma generate
  #     - name: Start Supabase local stack
  #       run: supabase start
  #     - name: Copy test env
  #       run: cp .env.test.example .env.test
  #     - name: Setup test database
  #       run: npm run db:test:setup
  #     - name: Run integration tests
  #       run: npm run test:integration
  #     - name: Stop Supabase
  #       if: always()
  #       run: supabase stop
